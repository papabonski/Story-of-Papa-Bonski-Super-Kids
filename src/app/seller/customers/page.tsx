import { createSupabaseAdminClient } from "@/lib/supabase/server"; import Link from "next/link";
export const dynamic="force-dynamic";
export default async function Page(){let rows:any[]=[];try{const {data}=await createSupabaseAdminClient().from("customers").select("id,customer_code,name,email,whatsapp,status,created_at").order("created_at",{ascending:false}).limit(100);rows=data||[]}catch{}
return <main className="min-h-screen bg-surface px-5 py-10 text-ink"><div className="mx-auto max-w-5xl"><Link href="/seller" className="text-sm font-bold text-brand-primary">← Seller Center</Link><h1 className="mt-3 text-3xl font-extrabold">Customers</h1><div className="mt-6 overflow-hidden rounded-2xl bg-surface-card ring-1 ring-black/[.06]"><div className="grid grid-cols-4 gap-3 border-b p-4 text-xs font-bold uppercase text-ink-soft"><span>Customer</span><span>Email</span><span>Status</span><span>Dibuat</span></div>{rows.map(r=><div key={r.id} className="grid grid-cols-4 gap-3 border-b border-black/5 p-4 text-sm"><span><b>{r.name}</b><small className="block text-ink-soft">{r.customer_code}</small></span><span>{r.email||"—"}</span><span>{r.status}</span><span>{new Date(r.created_at).toLocaleDateString("id-ID")}</span></div>)}{!rows.length&&<p className="p-6 text-sm text-ink-soft">Belum ada customer. Customer akan muncul otomatis setelah webhook pembayaran diproses.</p>}</div></div></main>}
